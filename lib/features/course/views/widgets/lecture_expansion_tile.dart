// Deprecated: This widget is no longer used in favor of the new flat tab-based CourseView.
// It will be removed in a future cleanup.
import 'package:flutter/widgets.dart';

class LectureExpansionTile extends StatelessWidget {
  const LectureExpansionTile({super.key});
  @override
  Widget build(BuildContext context) => const SizedBox.shrink();
}
